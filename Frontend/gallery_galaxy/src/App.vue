<template>
  <div id="app">
  <router-view></router-view>
  </div>
</template>

<script>
/*import { ref, onMounted } from 'vue';

export default {

  props: {
    // These props are automatically passed from the route
    baseUrl: String,
    getImageUrl: Function,
    searchResults: Array,
  },
  setup(props) {
    const localSearchResults = ref([]);

    onMounted(() => {
      // Get the search results from the route parameter
      localSearchResults.value = props.searchResults;
    });

    return {
      localSearchResults,
    };
  },
};*/
</script>


<style>
/* Your styles here */
</style>
